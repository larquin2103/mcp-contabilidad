// src/tools/escenarios.ts
// TODO: implementar modelación de escenarios financieros

export const escenarioTools: any[] = [];
export async function ejecutarEscenarios(name: string, args: any): Promise<string> {
  throw new Error(`Tool de escenarios pendiente de implementar: ${name}`);
}
